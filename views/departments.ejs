<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Departments</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #fafafa;
            padding: 30px;
            color: #333;
        }

        h2 {
            font-size: 26px;
            margin-bottom: 25px;
            font-weight: 700;
        }

        .search-container {
            display: flex;
            gap: 20px;
            margin-bottom: 25px;
        }

        input,
        select {
            width: 260px;
            padding: 14px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 15px;
        }

        table {
            width: 100%;
            background: white;
            border-collapse: collapse;
            border-radius: 12px;
            overflow: hidden;
        }

        th {
            background: #f0f0f0;
            padding: 16px;
            font-weight: 600;
            text-align: left;
            font-size: 15px;
        }

        td {
            padding: 16px;
            border-top: 1px solid #eee;
            font-size: 15px;
        }

        tr:nth-child(even) {
            background: #fafafa;
        }

        .edit-btn {
            background: #457b9d;
            color: white;
            padding: 8px 16px;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            font-size: 14px;
        }

        .delete-btn {
            color: #c1121f;
            font-size: 14px;
            cursor: pointer;
            border: none;
            background: none;
            margin-left: 10px;
        }
    </style>
</head>

<body>

    <h2>All Departments</h2>

    <div class="search-container">
        <form action="/departments" method="get">
            <input type="text" name="search" placeholder="Search by department name <%= search %>"  >
            <select name="type" >
                <option  value="all" <%= type==="all" ? "selected" : ""  %> >All</option>
                <option value="UG" <%= type==="UG" ? "selected" : ""  %>  >UG</option>
                <option value="PG" <%= type==="PG" ? "selected" : ""  %> >PG</option>
                <option value="Research" <%= type==="Research" ? "selected" : ""  %>  >Research</option>
            </select>
            <button type="submit">Apply</button>
            <button onclick="window.location.href = '/departments?search=&type=all' " >Reset</button>
        </form>
    </div>

    <table>
        <thead>
            <tr>
                <th>Department Name</th>
                <th>Type</th>
                <th>Number of Users</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            <!-- Hardcoded Rows -->
            <% data.forEach((ele)=>{ %>
                <tr>
                    <td>
                        <%=ele.name%>
                    </td>
                    <td>
                        <%=ele.type%>
                    </td>
                    <td>
                        <%=ele.userCount%>
                    </td>
                    <td>
                        <button onclick="window.location.href='/edit-department/<%=ele._id%>'">Edit</button>
                    </td>
                    <td>
                         <button onclick="window.location.href='/delete-department/<%=ele._id%>'">Delete</button>
                    </td>
                </tr>
                <%})%>
        </tbody>
    </table>

</body>

</html>