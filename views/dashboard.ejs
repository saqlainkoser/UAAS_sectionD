<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: #fafafa;
            color: #333;
        }

        header {
            padding: 20px 40px;
            font-size: 28px;
            font-weight: bold;
            border-bottom: 1px solid #ddd;
            background: white;
        }

        .container {
            display: flex;
            padding: 20px 40px;
            gap: 40px;
        }

        /* Sidebar */
        .sidebar {
            width: 220px;
        }

        .sidebar h3 {
            margin-bottom: 15px;
            font-size: 20px;
        }

        .nav-btn {
            width: 100%;
            padding: 12px;
            background: #e6e6e6;
            margin-bottom: 12px;
            border-radius: 8px;
            border: none;
            cursor: pointer;
            font-size: 16px;
            text-align: left;
            transition: 0.2s;
        }

        .nav-btn:hover {
            background: #d9d9d9;
        }

        /* Dashboard Content */
        .dashboard {
            flex: 1;
        }

        .dashboard h2 {
            margin-bottom: 20px;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(2, 250px);
            gap: 20px;
        }

        /* Cards */
        .card {
            padding: 20px;
            border-radius: 12px;
            background: white;
            border: 1px solid #eee;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        .card h4 {
            margin: 0;
            font-size: 18px;
            color: #555;
        }

        .card .value {
            margin-top: 12px;
            font-size: 36px;
            font-weight: bold;
        }

        /* Soft colors like image */
        .purple { background: #f3f0ff; }
        .green { background: #edf9ee; }
        .yellow { background: #fff6e5; }
        .lavender { background: #f7f0ff; }

        footer {
            text-align: center;
            padding: 20px;
            font-size: 14px;
            margin-top: 30px;
            border-top: 1px solid #ddd;
            color: #666;
        }
    </style>
</head>

<body>

    <header>Admin Dashboard</header>

    <div class="container">

        <!-- Sidebar -->
        <div class="sidebar">
            <h3>Navigation</h3>
            <button class="nav-btn">Departments</button>
            <button class="nav-btn">Users</button>
            <button class="nav-btn">Add User</button>
            <button class="nav-btn"><a href="/create-department">Create Department</a></button>
        </div>

        <!-- Main Dashboard -->
        <div class="dashboard">
            <h2>Dashboard Overview</h2>

            <div class="grid">

                <div class="card purple">
                    <h4>Total Departments</h4>
                    <div class="value">3</div>
                </div>

                <div class="card green">
                    <h4>Total Students</h4>
                    <div class="value">40</div>
                </div>

                <div class="card yellow">
                    <h4>Total Professors</h4>
                    <div class="value">2</div>
                </div>

                <div class="card lavender">
                    <h4>Total HODs</h4>
                    <div class="value">1</div>
                </div>

            </div>
        </div>

    </div>
    <p style="color:green;">
        <%if(message.length){%>
            <span><%=message%></span>
        <%}%>
    </p>
    <footer>© 2025 Admin Dashboard. All rights reserved.</footer>

</body>
</html>
